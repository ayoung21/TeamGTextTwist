#ifndef UTILS_H
#define UTILS_H

#include <string>
#include <vector>
#include <iostream>
using namespace std;

const string toUpperCase(string text);
void initalizeLetters(vector<string> &letterList);

#endif // UTILS_H
